import { ModifyDirective } from './modify.directive';

describe('ModifyDirective', () => {
  it('should create an instance', () => {
    const directive = new ModifyDirective();
    expect(directive).toBeTruthy();
  });
});
